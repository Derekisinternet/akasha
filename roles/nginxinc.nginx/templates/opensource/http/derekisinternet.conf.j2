server {
  listen        {{ vhost.port }} default_server;
  listen        [::]:{{ vhost.port }} default_server;
  root          {{ vhost.root}};
  index         {{ vhost.index }};
  server_name   {{ vhost.server_name }};
  
  location / {
    try_files  $uri $uri/ =404;
  }

  error_page 404 /http/404.html;
}
