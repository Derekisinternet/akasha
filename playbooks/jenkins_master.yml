---
- hosts: jenkins_master
  become: true
  roles:
    - base
    - git
    - geerlingguy.docker
  tasks:
    - name: download and run docker container
      docker_container:
        name: jenkins
        image: jenkins/jenkins:lts
        state: present
        exposed_ports:
          - 8080
          - 50000


